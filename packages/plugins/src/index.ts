/**
 * @cluster/plugins
 *
 * Storage provider implementations for the Cluster platform.
 *
 * Each provider implements the IStorageProvider interface from @cluster/core.
 *
 * Usage:
 *   import { SharePointProvider } from '@cluster/plugins/sharepoint';
 *   // or
 *   import { SharePointProvider } from '@cluster/plugins';
 */

// Re-export all providers for convenience
export * from './sharepoint';
